[pytest]
testpaths = tests
# `app` is the import root — tests do `from app.processing.extract import ...`, so pytest
# must be run with `server/` as the rootdir (which this file pins).
addopts = -q
filterwarnings =
    ignore::FutureWarning
    ignore::UserWarning
